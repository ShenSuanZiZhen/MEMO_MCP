# 模块化 MCP 构建套件产品 UI 设计手册

> 版本：V1.0  
> 日期：2026-09-15  
> 产品形态：全新独立产品  
> 设计范围：桌面端主创作流程、窄屏运营流程、系统组件与状态

---

## 1. 设计目标

UI 必须让用户始终回答五个问题：

1. 我当前在哪个 Workspace、Project 和 Environment；
2. 我正在编辑草稿、候选还是已发布版本；
3. 当前可以选择什么；
4. 这个选择会改变哪些数据、能力、访问或限制；
5. 如果不能继续，原因和可执行下一步是什么。

产品采用“业务语言优先、技术细节可展开”的双层表达。普通用户先看到“搜索资料、按段读取、验证引用”；开发者可展开 Tool 名称、JSON Schema 和 MCP 请求示例。

## 2. 设计原则

### 2.1 选择与结果同屏

创建向导右侧持续展示最终服务摘要。任何有效选择后立即刷新，不让用户到预览页才发现实际结果。

### 2.2 安全默认值可见

原文件下载、整篇读取、任意 SQL、用户脚本和跨项目访问不是隐藏开关，而是明确标为 P0 不支持或默认关闭，并说明原因。

### 2.3 阻断可解决

每个错误说明：发生了什么、影响什么、如何处理、请求或任务编号。阻断项可以一键定位到步骤和字段。

### 2.4 长任务可离开

上传、解析、索引、构建、测试和部署展示阶段进度，并允许用户安全离开。通知只在完成、失败或需要操作时出现。

### 2.5 状态比动画重要

不使用无限旋转图标代替信息。超过 10 秒的任务需要阶段、进度、后台运行说明和可恢复入口。

### 2.6 版本不可混淆

草稿、候选、生产版本和数据版本使用固定标签和颜色/图标组合。编辑新版本时持续显示线上仍运行的版本。

## 3. 信息架构

```text
全局
├── Workspace 切换
├── Project 切换
├── Environment 切换
├── 全局搜索
├── 任务中心
├── 通知
└── 用户与帮助

Project
├── 概览
├── MCP 服务
│   ├── 草稿
│   ├── 审核中
│   ├── 已发布
│   └── 已暂停/已下线
├── 数据
│   ├── 数据源
│   ├── 数据版本
│   ├── 上传任务
│   └── 连接凭证
├── 模块
├── 测试
├── 访问与凭证
├── 运行与告警
├── 审核
├── 成员与角色
└── 项目设置
```

平台运营后台独立导航，不和普通项目页面混在一起。

## 4. 全局布局

### 4.1 桌面框架

```text
┌────────────────────────────────────────────────────────────────────────────┐
│ Logo  Workspace ▾  Project ▾  Environment ▾       搜索 任务 通知 用户     │
├───────────────┬────────────────────────────────────────────────────────────┤
│ 项目导航      │ 页面标题 / 状态 / 主操作                                  │
│               ├────────────────────────────────────────────────────────────┤
│ 概览          │ 页面内容                                                   │
│ MCP 服务      │                                                            │
│ 数据          │                                                            │
│ 模块          │                                                            │
│ 测试          │                                                            │
│ 访问与凭证    │                                                            │
│ 运行与告警    │                                                            │
└───────────────┴────────────────────────────────────────────────────────────┘
```

### 4.2 创建向导框架

```text
┌────────────────────────────────────────────────────────────────────────────┐
│ ← 服务列表  产品资料查询 / 草稿   保存状态                保存并退出       │
├──────────────┬──────────────────────────────────┬──────────────────────────┤
│ ① 目标  ✓    │ 当前步骤主操作                   │ 当前服务结果             │
│ ② 数据  ●    │                                  │ 数据：3 个文件 / v1      │
│ ③ 模块       │                                  │ Tools：5                 │
│ ④ 配置       │                                  │ Resources：3             │
│ ⑤ 预览       │                                  │ Prompts：3               │
│ ⑥ 测试       │                                  │ 输出：10 条 / 5 段       │
│ ⑦ 发布       │                                  │ 不提供：原文件、整篇     │
│              │                                  │ 风险：低 / 阻断：0       │
├──────────────┴──────────────────────────────────┴──────────────────────────┤
│ 上一步                                        保存草稿  保存并进入下一步   │
└────────────────────────────────────────────────────────────────────────────┘
```

推荐宽度：左侧步骤 200–224 px，右侧摘要 300–340 px，中间自适应，内容最大宽度 1440 px。

## 5. 页面清单

| 编号 | 页面/弹层 | 主任务 |
| --- | --- | --- |
| UI-01 | 登录/企业身份跳转 | 建立用户会话 |
| UI-02 | Workspace 选择/创建 | 选择隔离空间 |
| UI-03 | Project 创建/选择 | 建立项目和环境 |
| UI-04 | 项目概览 | 查看服务、任务和告警摘要 |
| UI-05 | MCP 服务列表 | 筛选、继续、查看服务 |
| UI-06 | 创建方式 | 模板、空白、复制版本 |
| UI-07 | 向导步骤 1：目标 | 业务资料和能力目标 |
| UI-08 | 向导步骤 2：来源选择 | 上传、HTTP、对象存储、数据库 |
| UI-09 | 文件上传 | 选择、分片、恢复、元数据 |
| UI-10 | 数据处理 | 扫描、解析、索引和部分失败 |
| UI-11 | 数据预览/修正 | 字段、标题、分类和排除 |
| UI-12 | HTTP API 连接 | 认证、路径、Schema、测试 |
| UI-13 | 对象存储连接 | bucket/prefix 和对象范围 |
| UI-14 | 数据库只读连接 | 表/视图/字段和查询限制 |
| UI-15 | 向导步骤 3：模块 | 推荐、选择和模块详情 |
| UI-16 | 模块依赖/冲突 | 补齐、替代和解决方案 |
| UI-17 | 向导步骤 4：Tools | 输入、读取和输出配置 |
| UI-18 | Resources/Prompts | 资源和使用模板 |
| UI-19 | 访问策略 | 身份、范围、时间和配额 |
| UI-20 | 运行保护/计量 | 速率、并发、超时、统计 |
| UI-21 | 向导步骤 5：预览 | 所有者、使用者、客户端三视角 |
| UI-22 | 向导步骤 6：测试 | 配置、进度、结果和报告 |
| UI-23 | 向导步骤 7：提交 | 版本、说明、清单和冻结 |
| UI-24 | 审核进度/意见 | 修改、补材料、通过或拒绝 |
| UI-25 | 部署进度 | 阶段、失败、摘要和健康 |
| UI-26 | 发布成功 | URL、认证、能力和下一步 |
| UI-27 | 创建 API Key | 权限、有效期、配额和 Secret |
| UI-28 | 创建 OAuth Client | 回调、Scopes 和连接说明 |
| UI-29 | 服务概览 | 指标、状态、告警和操作 |
| UI-30 | 服务能力 | 线上 Tools/Resources/Prompts |
| UI-31 | 服务数据 | 数据源、版本和更新候选 |
| UI-32 | 访问与凭证 | 策略、凭证、轮换和撤销 |
| UI-33 | 请求追踪 | 脱敏阶段、错误和 requestId |
| UI-34 | 版本与差异 | 复制、比较、升级和回滚流量 |
| UI-35 | 高影响操作确认 | 暂停、停止新访问、下线 |
| UI-36 | 任务中心 | 后台任务、进度和恢复 |
| UI-37 | 通知中心 | 审核、部署、告警和到期 |
| UI-38 | 模块目录 | 查看平台可用模块 |
| UI-39 | 成员和角色 | 邀请、修改和移除 |
| UI-40 | 平台运营后台 | 模块、策略、资源和安全事件 |

每个页面至少设计：默认、加载、空、成功、阻断、可恢复失败、权限不足。列表页增加分页末尾和筛选无结果状态；任务页增加部分成功和已取消。

## 6. 全局组件

### 6.1 Workspace/Project/Environment 选择器

- 三者始终可见但视觉层级不同；
- 切换前若有未确认本地输入，先提示保存；
- 生产环境使用清晰标签和图标，不只使用红色；
- 搜索结果按最近、收藏和全部分组；
- 无权限的环境不显示可执行主操作。

### 6.2 状态标签

| 状态类别 | 建议表现 |
| --- | --- |
| 草稿 | 中性灰蓝、铅笔图标、文字“草稿” |
| 处理中 | 蓝色、进度图标、具体阶段文字 |
| 需要操作 | 橙色、感叹图标、文字说明 |
| 成功/运行 | 绿色、勾选/运行图标 |
| 暂停 | 紫/灰、暂停图标 |
| 失败/阻断 | 红色、错误图标、原因标签 |
| 已下线/归档 | 灰色、归档图标 |

不能只靠颜色表达。

### 6.3 选择卡片

用于模板、目标、来源和模块。卡片必须包含：名称、说明、适用场景、主要结果、限制/风险、选择状态和详情入口。

状态：默认、hover、focus、已选、部分选、推荐、强制、禁用、警告、阻断。

单选卡点击整卡生效；多选卡有明确 checkbox。详情按钮不能同时触发选择。

### 6.4 Effective Result Panel

固定分组：

1. 数据；
2. Tools；
3. Resources；
4. Prompts；
5. 输出；
6. 访问；
7. 不会提供；
8. 风险与问题。

每一项可点击定位来源配置。变化后短暂高亮变化项，但不得用动画阻止操作。

### 6.5 Issue Panel

问题按步骤分组，包含级别、标题、原因、影响、修复建议和“前往修改”。

```text
阻断 2 / 警告 1

步骤 3 · 模块
缺少“文档元数据”依赖
引用验证将无法确定引用版本。
[一键补齐] [查看模块]
```

警告可继续但需要查看；阻断禁用下一步。禁止提供“忽略所有问题”。

### 6.6 表单字段

- label 永远可见，不以 placeholder 替代；
- 必填标记和帮助文案靠近字段；
- 输入中只做格式提示，失焦或提交时给出完整错误；
- 错误包含允许范围和修复示例；
- Secret 字段有显示/隐藏切换，但保存后不回显；
- 高级 JSON 编辑器必须和表单双向校验，错误定位到 JSON path。

### 6.7 任务进度器

```text
建立索引                                              72%
✓ 上传  ✓ 扫描  ✓ 解析  ✓ 切块  ● 索引  ○ 验证
已处理 35 / 48 个段落，预计还需约 20 秒
[在后台继续] [查看详细日志]
```

取消时说明已经完成的产物和是否可以恢复。

### 6.8 差异查看器

分组显示新增、删除和变化：数据、模块、能力、Schema、输出、访问、风险。支持“只看高风险变化”和左右版本切换。

### 6.9 影响确认框

不使用简单“是否确定”。必须显示：动作、目标、立即影响、现有使用者/凭证、恢复方法、通知范围和操作者需要输入的原因。

## 7. 关键页面规格

### 7.1 UI-04 项目概览

首屏：

- “创建 MCP”主按钮；
- 服务状态卡；
- 后台任务；
- 运行指标；
- 需要处理；
- 最近活动。

首次空状态使用三张模板卡和“了解 7 个步骤”，不要用无内容表格。

### 7.2 UI-05 服务列表

筛选：状态、环境、模板、所有者、更新时间。列：服务名、生产版本、环境状态、数据版本、调用量、错误率、更新时间、操作。

行点击进入详情；“继续编辑”“创建新版本”“暂停”放入与状态匹配的行操作。不可用操作说明原因。

### 7.3 UI-06 创建方式

三个入口卡后显示模板区。模板详情抽屉包含最终能力示意、需要的数据、默认限制、预计步骤和适用/不适用场景。

### 7.4 UI-07 目标

页面由基础资料、能力卡片、禁止用途三部分构成。右侧结果即时显示能力数量和推荐数据要求。

不把 Tool 技术名作为卡片标题；技术名放次级说明。

### 7.5 UI-08 来源选择

四张来源卡并列；选择后展开相应说明。组合多个来源使用“已添加来源”列表，而非在卡片内不断嵌套表单。

切换或移除来源前显示受影响模块、能力和测试。

### 7.6 UI-09 文件上传

布局：上传区、文件列表、公共元数据、版本策略。

文件行显示：图标、名称、格式、大小、上传百分比、阶段、错误、重试/移除。大量文件时使用虚拟列表，上传区仍保留。

部分失败时页面主结论是“2 成功、1 需要处理”，不是笼统“上传失败”。

### 7.7 UI-10 数据处理

顶部聚合进度，下面按文件显示阶段。失败详情抽屉定位页码、工作表或字段。

允许操作：重试失败项、替换、排除、取消整个任务。确认排除前显示最终可用数据减少多少。

### 7.8 UI-11 数据预览

文档：目录 + 页面/段落；表格：工作表 + 字段 + 行；JSON：树形字段 + 样例。预览默认遮蔽敏感值。

允许用户修改标题、分类、标签、字段用途和排除项，不允许在这里直接编辑原始正文。

### 7.9 UI-12～UI-14 连接向导

使用四步子向导：连接信息 → 凭证 → 范围 → 测试确认。

连接测试输出分阶段，不把所有错误写成“连接失败”。保存后凭证字段替换为凭证摘要和“更新凭证”。

### 7.10 UI-15 模块选择

顶部推荐组合，下面按四类模块 tab。右侧继续使用 Effective Result Panel。模块详情使用抽屉，保留当前列表筛选和滚动位置。

### 7.11 UI-16 依赖/冲突

用关系列表或小型依赖图表达；只在超过三层关系时使用图。每个方案明确“会新增/移除什么”和“后续哪些配置需要重做”。

### 7.12 UI-17 Tools 配置

左侧 Tool 列表，中央配置，右侧实时 Schema/结果示例。每个 Tool 有“基础”和“高级”页签；高级不解锁额外权限。

### 7.13 UI-18 Resources/Prompts

Resource 卡显示 URI 模板和可见/可读范围；Prompt 卡显示输入和调用链。停用能力时相关项显示受影响状态并提供跳转。

### 7.14 UI-19 访问策略

采用规则构建器，但 P0 只提供受控条件：主体、角色、客户端、时间、环境、能力和配额。不提供任意表达式输入。

右侧提供“用情景测试策略”：选择模拟用户/凭证，立即显示允许、拒绝和原因。

### 7.15 UI-20 运行保护

每个数值并列展示：平台上限、项目上限、模块上限、用户选择、实际生效值。若用户输入更宽值，字段不自动悄悄改写，而是说明最终值及原因。

### 7.16 UI-21 三视角预览

三个 tab 共用 previewId 和摘要。顶部显示“任何关键修改会使本次预览确认失效”。

客户端视角提供可复制代码块，但凭证使用占位符。

### 7.17 UI-22 测试中心

左侧测试套件，中间用例表，右侧失败详情。表格可筛选失败、必测、权限、数据、输出和可靠性。

运行中不锁死页面。单项重跑显示它不会改变成功用例的历史记录。

### 7.18 UI-23 提交

页面不是重复所有表单，而是版本信息、变更摘要、检查清单、阻断面板和冻结说明。主按钮写“提交 1.0.0 审核”。

### 7.19 UI-24 审核

时间线展示审核阶段；意见卡显示步骤、字段、严重程度、原因、建议和“前往修改”。通过后显示部署入口。

### 7.20 UI-25/UI-26 部署与成功

部署使用阶段进度器。失败说明已完成阶段、是否已接流量、可否重试。摘要不一致时使用强阻断页面。

成功页突出服务 URL、版本、环境、认证、能力、数据版本和三个下一步：创建凭证、调试、查看详情。

### 7.21 UI-27 API Key

表单：名称、环境、能力范围、数据范围、有效期、配额和网络限制。创建成功页只显示一次完整 Secret；关闭前必须确认已安全保存。

之后的列表只显示名称、前缀、末四位、状态、创建人、最近使用和到期时间。操作：轮换、撤销、查看使用记录。

### 7.22 UI-28 OAuth Client

表单：客户端名称、应用类型、回调地址、Scopes、环境、Logo 可选。显示 client ID、Secret 展示一次、授权地址、Token 地址、MCP URL 和集成示例。

### 7.23 UI-29 服务概览

头部固定：服务名、版本、环境、状态、URL、认证、主操作。内容：调用趋势、成功/拒绝/错误、延迟、配额、近期告警、关联数据、近期发布。

生产服务主操作优先顺序：在调试器打开、创建新版本、管理凭证；暂停放入高影响菜单。

### 7.24 UI-30 服务能力

线上只读展示 Tools、Resources、Prompts 和最终 Schema。每项标注来源模块、版本和访问策略。可以复制示例，不能在页面直接编辑生产定义。

### 7.25 UI-31 服务数据

显示数据源、固定/更新策略、数据版本、处理状态、更新时间、范围和权利状态。主操作为“创建数据更新候选”，不提供覆盖线上数据的“重新上传”。

### 7.26 UI-32 访问与凭证

先显示访问策略，再显示 API Key/OAuth Client。撤销前展示最近使用和影响；轮换支持短暂双 Key 过渡期并显示截止时间。

### 7.27 UI-33 请求追踪

筛选：时间、环境、版本、Tool、凭证、状态、错误码。详情时间线：入口、认证、策略、数据、能力、输出、响应。

只显示参数字段名、大小和摘要，不显示完整查询、正文或 Secret。提供复制 requestId/traceId。

### 7.28 UI-34 版本与差异

版本时间线展示草稿、审核、发布、暂停、下线。差异按数据、模块、能力、输出、访问和风险分组。支持“只看高风险变化”。

### 7.29 UI-35 高影响确认

暂停、停止新访问和下线使用三套独立文案、图标和影响摘要。用户选择立即或预约时间，填写原因，确认影响后执行。

### 7.30 UI-36/UI-37 任务与通知

任务中心用于正在运行和可恢复的上传、处理、构建、测试和部署。通知中心只承载完成、失败、审核意见、告警和到期等事件。

相同任务连续进度不产生多条通知；一个通知可直达需要处理的对象和步骤。

## 8. 页面状态规范

### 8.1 通用状态

| 状态 | 表现 | 主操作 |
| --- | --- | --- |
| 初次空 | 解释价值、给模板/示例 | 创建或了解流程 |
| 筛选空 | 显示当前筛选条件 | 清除筛选 |
| 加载 | 骨架和明确区域 | 无，保留导航 |
| 短任务运行 | 行内 spinner + 状态文字 | 取消，可选 |
| 长任务运行 | 阶段、进度、可离开 | 在后台继续 |
| 成功 | 结果摘要和下一步 | 进入下一个核心动作 |
| 部分成功 | 成功/失败数量和影响 | 修复失败项或确认排除 |
| 可重试失败 | 保留输入和成功结果 | 重试 |
| 不可重试失败 | 原因和替代动作 | 替换、返回或联系支持 |
| 权限不足 | 所需角色，不展示内容 | 切换项目或联系管理员 |
| 数据变化 | 新旧版本和影响 | 刷新、比较或创建新版本 |
| 服务降级 | 可用/不可用能力 | 查看告警或暂停 |

### 8.2 表单错误

顺序：字段边框/图标 → 字段下错误 → 页面顶部问题摘要。点击顶部错误聚焦字段并保证错误说明进入视口。

### 8.3 后台任务失败

状态卡必须说明：失败阶段、已保存成果、是否自动重试、下次重试时间、可执行动作、jobId。

### 8.4 授权错误

| 状态 | 使用者文案 | 所有者可见信息 |
| --- | --- | --- |
| 未认证 | 请连接有效身份 | 认证失败计数，不显示身份细节 |
| 凭证无效 | 凭证无效或已过期 | 凭证前缀、状态和 requestId |
| 无能力权限 | 当前凭证不能使用此能力 | 策略规则和去标识化主体 |
| 超配额 | 已达到当前配额 | 配额桶、使用量和重置时间 |
| 服务暂停 | 服务暂时不可用 | 暂停原因、操作者和恢复入口 |

无权状态不告诉使用者指定文档或数据是否存在。

## 9. 交互细节

### 9.1 自动保存

- 输入停止 1–2 秒后保存；
- 网络失败指数退避，顶部显示失败和“立即重试”；
- 服务端确认后才显示“已保存”；
- 离线输入标记“仅保存在此设备”；
- 关闭页面前存在未同步内容时提示影响；
- 版本冲突打开 diff，不自动覆盖。

### 9.2 异步刷新

- 预计少于 10 秒：页面内刷新；
- 超过 10 秒：显示后台运行；
- 任务进度采用渐进退避，不让页面高频闪动；
- 完成时若用户仍在页面，原位更新并辅助技术播报；
- 用户离开后通过任务中心和通知更新。

### 9.3 表格

- 默认行高 48–56 px；
- 标题列固定，横向滚动时保留关键操作；
- 筛选、排序和列显示状态写入 URL；
- 批量操作只在选择后出现，并说明适用范围；
- 删除/撤销等高影响操作不放入误触区域；
- 空数据和加载不用相同视觉。

### 9.4 抽屉和弹层

- 模块详情、失败详情和字段帮助使用抽屉；
- 高影响确认、Secret 单次展示使用 modal；
- 复杂编辑不在小 modal 中完成，跳转专用页面；
- 关闭抽屉恢复触发元素焦点和列表位置；
- modal 禁止背景操作并支持 Escape，关键提交进行中除外。

### 9.5 复制

URL、ID、Schema、示例和 Secret 提供复制反馈。普通复制成功提示 2 秒；Secret 复制同时提醒安全保存，不写剪贴板日志。

## 10. 文案规范

### 10.1 术语

| 技术词 | 页面主文案 | 简短解释 |
| --- | --- | --- |
| MCP Server | MCP 服务 | 可被兼容客户端连接和调用的服务 |
| Tool | 可调用能力（Tool） | 客户端能执行的具体动作 |
| Resource | 可访问资源（Resource） | 可以枚举或读取的结构化入口 |
| Prompt | 使用模板（Prompt） | 帮助按预设方式使用能力，不新增权限 |
| Schema | 参数/结果结构 | 允许输入什么、会返回什么 |
| Module | 模块 | 可组合的数据、能力、输出或模板单元 |
| Definition | 服务定义 | 本版本数据、模块、能力和限制的完整描述 |
| Candidate | 候选版本 | 已冻结并等待审核的服务定义 |
| Deployment | 部署 | 某版本在一个环境中的运行实例 |
| Credential | 访问凭证 | 客户端证明身份和访问范围的 Key 或 OAuth Client |

首次出现使用“中文（技术词）”，后续业务页面用中文，开发者页面可技术名优先。

### 10.2 按钮

使用动作和结果：

- “上传并处理”，不写“确定”；
- “保存并选择模块”，不只写“下一步”；
- “提交 1.0.0 审核”，不只写“提交”；
- “创建数据更新候选”，不写可能表示覆盖的“更新数据”；
- “暂停所有新调用”，不只写“暂停”；
- “撤销此 API Key”，不只写“删除”。

### 10.3 错误结构

推荐：

```text
有 2 个 PDF 页面无法解析，因此当前数据版本还不能完成。
已成功处理的 40 个页面会保留。你可以重试、替换文件或排除这 2 页。
任务编号：job_01...
[查看失败页面] [重试]
```

不推荐：

```text
处理失败。Error 500。
```

### 10.4 风险层级

| 层级 | 定义 | 行为 |
| --- | --- | --- |
| 信息 | 解释产品或技术概念 | 可关闭，不影响流程 |
| 建议 | 存在更合适默认值 | 推荐但可保留用户选择 |
| 警告 | 可能扩大数据、成本或审核 | 需要查看影响后继续 |
| 阻断 | 违反权限、安全、依赖或发布规则 | 禁用继续并给修复动作 |

## 11. 视觉系统建议

本手册不指定品牌色，视觉设计应建立语义 Token，避免直接在页面硬编码颜色。

### 11.1 Token 层级

```text
Primitive: color.blue.500 / space.4 / radius.8
Semantic: surface.default / text.muted / border.danger
Component: button.primary.bg / status.blocked.text
```

### 11.2 颜色

至少提供：背景、表面、边框、主文字、次文字、链接、品牌、成功、信息、警告、危险、生产环境和焦点颜色。所有正文和交互状态达到 AA 对比度。

### 11.3 字体

- 正文最小 14 px，主要内容推荐 14–16 px；
- 页面标题 24–32 px；
- 表格、代码、ID 和 Schema 使用可读等宽字体；
- 中文和英文混排保持统一行高；
- 不通过过细字重表达次要信息。

### 11.4 间距与密度

使用 4 px 基础网格。表单区宽松，数据表可使用舒适/紧凑两档。复杂页面通过分组、折叠和 sticky 区域降低滚动，不通过缩小字号塞入更多内容。

### 11.5 图标

图标必须配文字用于状态和关键操作。来源、模块类型、状态和风险各自使用不同图标语义，避免一个警告图标承载所有异常。

## 12. 响应式设计

| 宽度 | 行为 |
| --- | --- |
| ≥ 1440 px | 完整三栏向导，右侧摘要固定 |
| 1280–1439 px | 三栏缩窄，表格按优先级隐藏次要列 |
| 1024–1279 px | 左侧步骤压缩，结果摘要变为可固定抽屉 |
| 768–1023 px | 支持查看、审批、任务和运营；复杂编辑提示桌面 |
| < 768 px | 支持通知、状态、暂停和凭证撤销；不承诺完整构建 |

小屏限制必须在进入复杂页面前说明，不能等用户填写一半才阻止。

## 13. 无障碍

- 跳过导航、主内容 landmark 和一致标题层级；
- 所有卡片选择可用键盘和屏幕阅读器；
- 拖拽上传同时提供标准文件选择；
- 状态变化使用非打扰式 live region；
- 进度提供数值和阶段文本；
- 表格表头、排序和选择状态语义完整；
- 错误与字段用 `aria-describedby` 关联；
- 焦点不被自动保存和后台刷新抢走；
- 图表提供文本摘要和数据表；
- 动画遵循减少动态效果设置。

## 14. 原型任务链

可点击原型必须覆盖：

### A. 上传并首次发布

```text
项目概览 → 资料查询模板 → 目标 → 上传 3 文件 → 处理成功
→ 模块推荐 → 配置 → 三视角预览 → 完整测试 → 审核 → 部署
→ 创建 API Key → 测试连接
```

### B. 处理失败文件

```text
3 文件上传 → 2 成功、1 解析失败 → 查看失败页 → 替换
→ 只重跑失败文件 → 数据版本完成
```

### C. 模块依赖与冲突

```text
启用引用验证 → 缺 2 个依赖 → 一键补齐
→ 选择冲突实时模块 → 比较方案 → 保留固定版本 → 校验通过
```

### D. 审核退回

```text
审核意见 → 定位“总正文长度” → 修改 4000 为 2000
→ 重跑受影响测试 → 查看差异 → 提交新修订
```

### E. 发布数据 v2

```text
服务详情 → 创建新版本 → 数据更新候选 → 查看差异
→ 预览/测试/审核 → 部署 1.1.0 → 旧版保留
```

### F. 凭证撤销

```text
访问与凭证 → 查看最近使用 → 撤销 → 影响确认
→ 客户端下一请求被拒绝 → 审计中可查看撤销记录
```

## 15. 设计交付物

设计团队至少交付：

1. 信息架构和导航图；
2. 用户旅程和六条任务链；
3. UI-01～UI-40 低保真关键状态；
4. 创建向导高保真桌面稿；
5. 运营、凭证、异常和高影响操作高保真稿；
6. 组件库、Token、图标和状态规范；
7. 空、加载、成功、部分成功、错误和权限状态；
8. 1280、1024、768 三档响应式说明；
9. 键盘、焦点、ARIA 和对比度说明；
10. 文案表、错误码呈现和术语表；
11. 可点击原型及用户测试记录；
12. 页面—PRD 需求编号映射。

## 16. UI 设计验收清单

### 16.1 范围和流程

- [ ] UI-01～UI-40 均有设计；
- [ ] 六条原型任务链可以连续完成；
- [ ] 每个创建步骤显示选择和结果；
- [ ] 上传、连接、模块、配置、预览、测试、审核和部署可恢复；
- [ ] 草稿、候选、版本和环境不会混淆。

### 16.2 安全和访问

- [ ] Secret 只显示一次，后续只见摘要；
- [ ] 无权错误不泄露资源存在性；
- [ ] 平台安全保护不可被 UI 关闭；
- [ ] 生产环境和高影响操作足够醒目；
- [ ] 暂停、停止新访问和下线明确区分；
- [ ] URL 旁说明仍需凭证和策略授权。

### 16.3 状态和错误

- [ ] 每页有默认、加载、空、成功、阻断和恢复失败状态；
- [ ] 部分成功明确显示成功与失败数量；
- [ ] 长任务可以离开和恢复；
- [ ] 错误包含影响、动作和标识；
- [ ] 问题可以定位到具体字段；
- [ ] 状态不只依赖颜色。

### 16.4 可用性

- [ ] 普通用户不理解 MCP 术语也能完成模板；
- [ ] 高级配置不会解锁额外权限；
- [ ] 核心任务可用键盘完成；
- [ ] 1280 px 完整可用，1024 px 无信息丢失；
- [ ] 对比度、焦点、屏幕阅读器和减少动画符合要求；
- [ ] 设计稿中的每个主操作都有明确成功和失败结果。

本 UI 手册定义表现和交互，不改变 PRD 中的数据、权限、版本和审核规则。任何设计创新若扩大能力或数据范围，应先作为产品需求变更评审。
