# 模块化 MCP 构建套件用户操作流程

> 版本：V1.0  
> 日期：2026-09-15  
> 产品形态：全新独立产品  
> 目标读者：产品经理、交互设计师、UI 设计师、用户研究、测试和研发团队

---

## 1. 文档目标

本文逐步说明用户在产品中做什么、可以选择什么、系统立即反馈什么、每一步形成什么结果，以及最终 MCP 服务如何被连接和运营。

主流程以“上传资料并构建受控查询 MCP”为贯穿案例，同时说明外部 API、对象存储和只读数据库等分支。

## 2. 用户旅程总览

```text
首次进入
  → 注册/登录
  → 创建或选择 Workspace
  → 创建 Project
  → 选择环境
  → 创建 MCP
      1. 定义目标
      2. 添加数据
      3. 选择模块
      4. 配置能力与访问
      5. 预览
      6. 测试
      7. 提交与发布
  → 创建客户端凭证
  → 复制连接信息
  → 查看调用、告警和版本
  → 创建新版本或暂停服务
```

## 3. 首次进入

### 3.1 注册或登录

用户可以选择：

- 邮箱登录；
- 企业身份登录；
- 已配置的第三方身份提供方。

系统结果：

- 登录成功后进入 Workspace 选择；
- 首次登录需要接受服务条款、数据处理说明和安全责任说明；
- 登录失败显示原因类别和重试/切换方式；
- 如果从邀请链接进入，登录后回到对应 Workspace 和 Project。

### 3.2 创建或选择 Workspace

用户选择：

| 选择 | 需要填写 | 结果 |
| --- | --- | --- |
| 个人空间 | 显示名称 | 只有本人管理，后续可升级团队 |
| 团队空间 | 团队名称、地区、用途 | 可邀请成员并分配角色 |
| 加入已有空间 | 接受邀请 | 获得邀请中声明的角色 |

系统在右上角始终显示当前 Workspace。切换 Workspace 会切换项目、数据、服务和凭证，不保留跨空间选择。

### 3.3 创建 Project

字段：

- 项目名称；
- 项目说明；
- 默认数据区域；
- 默认环境：开发；
- 成员和角色，可稍后设置。

结果：创建隔离的项目空间，进入项目首页。首次项目首页展示“创建第一个 MCP”和三个示例模板。

### 3.4 项目首页

用户可看到：

- 服务总数和状态；
- 草稿和需要处理的审核；
- 数据连接和近期处理任务；
- 调用量、错误率和告警；
- 最近操作；
- “创建 MCP”“添加数据”“邀请成员”三个主操作。

## 4. 创建入口

用户点击“创建 MCP”后选择：

| 方式 | 适用场景 | 选择结果 |
| --- | --- | --- |
| 使用模板 | 第一次使用或标准场景 | 预选目标、模块、能力和安全默认值 |
| 从空白开始 | 已清楚知道需要哪些能力 | 只启用平台强制保护 |
| 复制已有版本 | 基于线上服务升级 | 复制配置和数据绑定，不复制凭证和实时用量 |

P0 模板：

1. 资料查询：搜索、按段读取、引用验证；
2. 数据验证：查看元数据、检查摘要和可用状态；
3. 目录浏览：列出对象并查看元信息，不返回正文。

选择模板后，页面先展示模板影响：预计 Tools、Resources、Prompts、数据要求、默认限制、不会提供的能力。用户确认后才创建 Draft。

## 5. 创建向导通用规则

### 5.1 七个步骤

```text
① 目标 → ② 数据 → ③ 模块 → ④ 配置 → ⑤ 预览 → ⑥ 测试 → ⑦ 发布
```

### 5.2 每页固定结构

```text
顶部：项目 / 环境 / 草稿名称 / 保存状态 / 保存并退出
左侧：七步进度、每步状态、阻断数量
中间：当前步骤主操作
右侧：当前服务结果
底部：上一步 / 保存草稿 / 保存并进入下一步
```

右侧“当前服务结果”始终显示：

- 已选择的数据和版本；
- 最终 Tools、Resources、Prompts；
- 使用者可以看到什么；
- 明确不会提供什么；
- 返回条数和长度；
- 认证和访问规则；
- 风险等级；
- 当前警告和阻断。

### 5.3 保存和返回

- 有效字段停止输入 1–2 秒后自动保存；
- 顶部显示“保存中”“已保存于 10:25”“保存失败”；
- 离开后返回同一草稿和上次步骤；
- 本步校验通过才能标记完成，但用户可保存未完成草稿；
- 返回修改影响后续结果时，先说明哪些推荐、测试或预览会失效；
- 多人冲突不自动覆盖，用户比较差异后选择刷新或复制新草稿。

## 6. 第一步：定义目标

### 6.1 填写基础资料

| 字段 | 用户选择/输入 | 即时反馈 | 结果 |
| --- | --- | --- | --- |
| 服务名称 | 2–60 个字符 | 重名、长度和保留词校验 | 显示在服务列表和 MCP 信息中 |
| 一句话说明 | 说明服务解决什么问题 | 显示推荐写法 | 用于使用者和客户端预览 |
| 适用对象 | 内部成员、指定客户、所有获授权用户 | 影响默认访问策略 | 形成受众范围 |
| 禁止用途 | 批量导出、反向识别等 | 安全规则建议 | 进入审核和使用说明 |
| 默认语言 | 中文、英文或多语言 | 影响模块文案和搜索 | 形成服务语言设置 |

### 6.2 选择业务能力

用户可多选：

| 能力 | 用户最终可以做什么 | 系统推荐结果 |
| --- | --- | --- |
| 浏览目录 | 列出授权范围内对象 | 推荐目录和元数据模块 |
| 查看元数据 | 查看标题、分类、版本、来源 | 推荐元数据 Tool |
| 搜索内容 | 输入关键词获得命中摘要 | 推荐索引、搜索和引用模块 |
| 按段读取 | 读取指定命中段落 | 推荐受控读取和输出限制 |
| 验证引用 | 验证引用属于当前版本 | 推荐 citation 模块 |

P0 的写入、删除、执行操作、整篇读取和原文件下载显示为“暂不支持”，不提供隐藏高级入口。

### 6.3 选择结果示例

用户选择“搜索内容 + 按段读取 + 验证引用”后，右侧更新：

```text
预计能力：5 Tools
预计资源：集合、文档、章节
预计模板：3 Prompts
默认输出：最多 10 条、最多 5 个段落、包含引用
明确关闭：原文件下载、整篇读取、数据写入
下一步需要：可解析的文本或结构化数据
```

### 6.4 完成本步

点击“保存并添加数据”。如果选择之间冲突，按钮保持可见但不可继续，并在字段下说明如何调整。

## 7. 第二步：添加数据

### 7.1 选择来源

| 来源 | 用户需要准备 | 适合场景 | 选择后进入 |
| --- | --- | --- | --- |
| 上传文件 | 本地文件和数据权利确认 | 文档、表格、固定资料 | 上传与处理 |
| HTTP API | 地址、认证、路径和响应样例 | 持续更新的业务接口 | 连接向导 |
| 对象存储 | endpoint、bucket、prefix、凭证 | 大量已有对象 | 范围选择 |
| 只读数据库 | 数据库地址、只读账户、表/视图 | 结构化业务数据 | Schema 选择 |

一个服务可以组合多个同类或不同来源，但 P0 单草稿默认最多 10 个数据来源。

### 7.2 上传文件流程

#### A. 选择文件

- 拖拽或文件选择器；
- 支持 TXT、MD、PDF、DOCX、CSV、XLSX、JSON；
- 默认单文件 100 MB、最多 20 个、单草稿 500 MB；
- 每个文件立即显示格式、大小、校验结果和移除按钮。

#### B. 设置公共元数据

- 数据集名称；
- 分类；
- 语言；
- 标签；
- 敏感等级：公开、内部、机密；
- 权利声明：本人拥有、已获授权、仅限内部使用；
- 权利到期时间，可选。

敏感等级选择“机密”后，系统自动收紧预览和日志，并提示可能需要更高级审核。

#### C. 选择版本策略

| 选项 | 结果 |
| --- | --- |
| 固定版本（默认） | 已发布服务永远读取本次确认的数据快照 |
| 发布前手动确认更新 | 数据可形成新候选，但用户确认和发布前不影响服务 |
| 受控跟随更新 | 仅在连接结构和数据范围未扩大时更新；扩大范围仍需新版本 |

#### D. 上传和后台处理

每个文件依次显示：

```text
上传 → 安全扫描 → 格式识别 → 解析 → 规范化 → 切块 → 建立索引 → 完成
```

用户可以离开页面。项目任务中心继续显示进度，完成、部分失败或需要处理时通知。

#### E. 处理结果

成功结果示例：

```text
3 个文件处理完成
共 48 个可读取段落，126 条表格记录
识别字段：title、body、category、source、page
警告：2 个 PDF 页面没有可提取文字，已标记为空页
[预览数据] [修正结构] [添加更多数据] [继续]
```

部分失败时按文件/页/表列出：

- 重试失败项；
- 替换文件；
- 移除文件；
- 确认排除失败部分。

成功项不重跑。用户确认排除前，数据版本不能标记为完成。

### 7.3 HTTP API 连接流程

用户依次设置：

1. 连接名称和用途；
2. 固定 Base URL；
3. 认证方式：无、API Key、Bearer、Basic、OAuth Client；
4. 允许的方法和固定路径模板；
5. 请求参数及默认值；
6. 分页方式；
7. 超时和单次最大响应；
8. 响应字段映射；
9. 固定快照或受控实时读取；
10. 运行连接测试。

测试结果分为认证、网络、响应状态、Schema 和分页。凭证保存后只显示类型、末四位和更新时间，不再回显。

### 7.4 对象存储流程

用户设置 endpoint、区域、bucket、允许 prefix、只读凭证和对象格式，然后执行：

- 连接测试；
- 列表范围预览；
- 抽样读取；
- 格式识别；
- 选择固定对象清单或受控 prefix；
- 确认最大对象数量和大小。

MCP 调用参数不能修改 bucket、endpoint 或 prefix。

### 7.5 只读数据库流程

用户选择数据库类型、输入只读连接、测试后选择：

- 允许的数据库/schema；
- 允许的表或视图；
- 可查询字段；
- 可过滤字段；
- 主键/稳定游标；
- 单次最大行数；
- 数据更新时间策略。

用户不能输入任意 SQL。系统生成受控查询模板并显示最终可触达范围。

### 7.6 数据预览与确认

每个数据源都提供：

- 元数据摘要；
- 脱敏样例；
- 字段和类型；
- 可查询/可返回候选；
- 版本和更新时间；
- 数据范围；
- 警告、失败和权利状态。

用户勾选“数据范围和权利说明正确”后点击“保存并选择模块”。

## 8. 第三步：选择模块

### 8.1 推荐区域

系统根据目标、数据类型和安全等级给出推荐组合：

```text
推荐组合：受控资料查询
Source：上传资料源
Capability：目录、元数据、搜索、按段读取、引用验证
Output：字段白名单、数量限制、敏感信息处理、引用
Prompt：检索回答、证据总结、引用验证
平台保护：身份、访问策略、配额、审计（始终启用）
```

用户可以：

- 接受全部推荐；
- 展开逐项选择；
- 查看模块详情；
- 比较可替代模块；
- 恢复推荐组合。

### 8.2 模块卡片

每张卡片显示：

- 中文名称和技术标识；
- 提供的能力；
- 精确版本；
- 风险等级；
- 依赖和冲突；
- 适用数据；
- 配置项数量；
- 审核状态；
- 对最终 MCP 的影响。

### 8.3 依赖与冲突

缺少依赖时：

```text
“引用验证”需要“按段读取”和“文档元数据”。
[一键补齐 2 个模块] [取消引用验证] [查看原因]
```

冲突时：

```text
“固定数据版本”与“实时无版本读取”不能同时使用。
方案 A：保留固定版本，移除实时读取（推荐）
方案 B：切换受控实时来源，并重新确认数据范围
```

用户必须明确选择解决方案。阻断问题不能通过“忽略”继续。

### 8.4 完成本步

页面生成最终模块清单、能力变化和风险摘要。点击“保存并配置能力”。

## 9. 第四步：配置能力与访问

本步骤使用分区式页面，不把所有配置堆在一个长表单中。

### 9.1 配置 Tools

#### `list_documents`

用户选择：

- 是否启用；
- 可用分类；
- 单页数量，默认 20；
- 返回字段：ID、标题、分类、版本、更新时间；
- 排序方式。

结果：使用者只能枚举访问策略允许的数据对象，不返回正文和原始位置。

#### `get_document_metadata`

用户选择：

- 返回标题、说明、分类、标签、语言、版本、来源、更新时间中的哪些字段；
- 是否返回完整性摘要；
- 是否显示数据更新时间。

结果：页面即时生成输入和输出 Schema。

#### `search_documents`

用户选择：

- 查询字段：title、body 等；
- 过滤字段：category、source 等；
- 查询词长度；
- 默认/最大结果数，默认和最大建议 10；
- 单条摘要长度；
- 是否返回页码和引用；
- 无结果时的响应文案。

结果：预览示例即时变化，关闭字段不会再出现在结果中。

#### `read_document_sections`

用户选择：

- 允许按段读取；
- 单次最多段落，P0 最大 5；
- 每段和总字符上限；
- 返回标题、页码、来源和引用；
- 整篇读取与原文件下载在 P0 保持关闭。

结果：使用者必须先获得有效 documentId/sectionId，不能用路径或存储地址读取。

#### `verify_citation`

用户选择：

- 是否启用；
- 返回版本、定位和摘要是否一致；
- 引用失效时的说明。

结果：验证只确认当前用户有权访问的引用，不扩大权限。

### 9.2 配置 Resources

可选资源：

```text
mcp://collections/{collectionId}
mcp://documents/{documentId}
mcp://documents/{documentId}/sections/{sectionId}
```

用户选择是否允许枚举集合、文档元数据，以及是否允许读取章节内容。系统同步展示 Resource 和 Tool 的访问范围是否一致。

### 9.3 配置 Prompts

用户可启用、停用或修改展示文案：

- 检索并回答；
- 按证据总结；
- 验证引用。

每个 Prompt 显示会调用的 Tools、用户仍需提供的参数和最大执行范围。停用某个 Tool 会自动提示哪些 Prompt 将失效。

### 9.4 配置访问策略

用户选择：

| 维度 | 选项 | 结果 |
| --- | --- | --- |
| 访问对象 | 项目成员、指定用户/团队、持有凭证者 | 决定谁可建立请求身份 |
| 认证方式 | API Key、OAuth | 决定客户端连接方式 |
| 能力范围 | 全部或指定 Tools/Resources | 凭证只能看到允许能力 |
| 有效期 | 永久、开始/结束时间 | 过期后新请求拒绝 |
| 环境 | 开发、测试、生产 | 凭证不能跨环境 |
| 配额 | 请求数、读取量 | 超出后返回配额错误 |
| IP/网络 | 不限制、允许列表 | 在身份策略之外增加网络限制 |

默认不在构建时创建生产凭证，只建立服务访问策略。发布后由有权限的管理员创建和分发凭证。

### 9.5 配置运行保护

- 每分钟请求数；
- 最大并发；
- 单请求超时；
- 响应最大大小；
- 敏感字段移除或遮蔽；
- 日志级别；
- 请求正文是否允许进入临时调试，仅开发环境可选且默认关闭；
- 异常时默认行为，固定为拒绝受保护请求。

平台限制、项目限制、模块限制和用户值取最严格结果。页面同时显示“你的选择”和“实际生效值”。

### 9.6 配置计量

用户选择运营维度，不在此步骤定价：

- 成功调用次数；
- 搜索命中数；
- 读取字节；
- 输出字节；
- 失败和拒绝只统计、不计费。

结果：形成用量统计口径，展示在发布后概览和凭证配额中。

### 9.7 本步完成结果

页面生成自然语言摘要：

```text
获授权使用者可搜索 3 份固定版本资料，单次最多返回 10 条，
可读取最多 5 个命中段落，并获得来源和引用。
使用者不能下载原文件、读取整篇、修改数据或访问其他项目资料。
服务使用 OAuth 或受限 API Key，每分钟最多 60 次，并发最多 5。
```

用户确认摘要后进入预览。

## 10. 第五步：三视角预览

### 10.1 所有者视角

展示：数据来源和版本、模块版本、Tools/Resources/Prompts、有效限制、访问策略、计量、风险、警告和阻断。

用户可点击任意项返回对应配置位置。

### 10.2 使用者视角

可切换情景：

- 未登录；
- 已登录但无访问权限；
- 有效访问；
- Tool 不在权限内；
- 超配额；
- 凭证过期；
- 服务暂停。

每种情景显示可见能力、调用结果和下一步，不展示内部模块和存储信息。

### 10.3 MCP 客户端视角

展示：

- Server 名称、版本和说明；
- 认证方式；
- Tools 列表、Schema 和调用样例；
- Resource Templates；
- Prompts；
- 成功、无权、超限和无结果示例；
- 协议连接示例。

### 10.4 确认

用户勾选：

- 能力与预期一致；
- 数据范围正确；
- 不会提供的内容正确；
- 访问和限制正确。

确认绑定当前 Preview 版本。后续修改任何关键配置会使确认失效。

## 11. 第六步：测试

### 11.1 选择测试类型

| 类型 | 用途 | 结果 |
| --- | --- | --- |
| 快速测试 | 验证 Schema、模块装配和基本调用 | 几分钟内给出基础报告 |
| 完整测试 | 发布前必需，覆盖权限、越界、限制和异常 | 所有必测项通过才能提交 |
| 自定义测试 | 添加业务问题、期望字段和禁止内容 | 与固定候选定义一起保存 |

### 11.2 完整测试用例

至少包含：

1. 列表、元数据、搜索、读取和引用验证正常调用；
2. 未认证；
3. 无访问权限；
4. Tool 不在凭证范围；
5. 猜测其他项目 documentId；
6. 请求超过 10 条；
7. 请求超过 5 个段落；
8. 索要整篇或原文件；
9. 数据不存在；
10. 数据连接超时；
11. 敏感字段出现在来源结果；
12. 超配额；
13. 凭证撤销；
14. 同一请求重试；
15. 模块或数据版本不一致。

### 11.3 测试过程

页面显示阶段、完成数量、失败数量、预计等待。用户可离开；后台继续执行。

失败项显示：用例目的、输入摘要、预期、实际、失败阶段、错误码、requestId 和建议修改位置。正文和 Secret 不进入报告。

### 11.4 测试结果

```text
完整测试：20 / 20 通过
能力正确性       5 / 5
数据边界         4 / 4
访问控制         5 / 5
输出保护         3 / 3
可靠性与重试     3 / 3

[查看报告] [下载安全摘要] [进入发布]
```

修改数据、模块、Schema、输出或访问范围后，仅重跑受影响项，但提交时必须形成一份完整通过报告。

## 12. 第七步：提交、审核与发布

### 12.1 发布信息

用户填写：

- 版本号，首次默认 `1.0.0`；
- 发布说明；
- 兼容性说明；
- 使用者需要注意的变化；
- 目标环境。

### 12.2 提交清单

- 数据权利与范围已确认；
- 模块版本已固定；
- Tools/Resources/Prompts 已确认；
- 整篇和原文件符合 P0 默认关闭；
- 访问策略和配额已确认；
- 三视角预览已确认；
- 完整测试已通过；
- 发布说明已填写。

### 12.3 冻结确认

```text
提交后，本次候选配置将被冻结。
审核期间需要修改时，会创建新的候选修订；
已发布版本不会被后续数据或模块更新静默改变。
[返回检查] [提交 1.0.0 审核]
```

### 12.4 审核

审核阶段：业务信息、数据与权利、技术配置、安全与输出、部署前检查。

审核结果：

| 状态 | 用户可做什么 |
| --- | --- |
| 审核中 | 查看进度，必要时撤回 |
| 需要修改 | 点击意见直接进入具体步骤和字段 |
| 补充材料 | 上传材料并创建修订 |
| 拒绝 | 查看原因、申诉或复制为新草稿 |
| 通过 | 确认部署到目标环境 |

### 12.5 部署

显示阶段：生成不可变定义、准备模块、准备数据绑定、启动实例、加载版本、健康检查、验证能力、开放 endpoint。

如果运行定义与审核定义不一致，状态为“部署被阻止”，不提供忽略继续。

### 12.6 发布成功

```text
服务已发布

名称：产品资料查询
版本：1.0.0
环境：生产
状态：运行正常
MCP URL：https://mcp.example.com/v1/services/svc_xxx/mcp
认证：OAuth / API Key
能力：5 Tools / 3 Resources / 3 Prompts
数据：资料库固定版本 v1

[创建访问凭证] [在调试器中打开] [查看服务详情]
```

URL 旁显示：“获得 URL 不代表获得访问权，客户端仍需有效凭证和策略授权。”

## 13. 创建凭证并连接 MCP

### 13.1 选择凭证类型

| 类型 | 适合场景 | 用户设置 |
| --- | --- | --- |
| API Key | 服务间快速接入、开发测试 | 名称、环境、能力、有效期、配额、网络限制 |
| OAuth Client | 多用户客户端和正式集成 | 客户端名称、回调地址、Scopes、环境、有效期策略 |

### 13.2 API Key

创建成功只显示一次完整 Secret：

```text
Key 名称：内部知识助手
前缀：mcp_live_x8K2
完整 Secret：mcp_live_x8K2_****************

请立即复制。关闭后无法再次查看，只能轮换。
[复制] [我已安全保存]
```

### 13.3 连接说明

页面提供：

- MCP URL；
- 认证 Header；
- 适配常见客户端的 JSON 配置示例；
- curl/raw HTTP 测试；
- 可见 Tools 列表；
- “测试连接”按钮。

示例只显示占位 Secret，不把真实 Key 写入可下载配置或日志。

### 13.4 客户端结果

认证和访问状态：

| 状态 | 客户端结果 |
| --- | --- |
| 无凭证 | 要求认证 |
| 凭证无效/过期 | 拒绝并提示更新凭证 |
| 凭证有效但无 Tool 权限 | Tool 不可见或调用被拒绝 |
| 有效且在配额内 | 正常执行 |
| 超配额 | 返回配额错误和可披露重置时间 |
| 服务暂停 | 返回服务暂不可用 |

## 14. 最终构建出的 MCP

### 14.1 Tools

```text
list_documents
get_document_metadata
search_documents
read_document_sections
verify_citation
```

### 14.2 Resources

```text
mcp://collections/{collectionId}
mcp://documents/{documentId}
mcp://documents/{documentId}/sections/{sectionId}
```

### 14.3 Prompts

```text
search_and_answer
summarize_with_evidence
verify_citations
```

### 14.4 调用示例

```json
{
  "name": "search_documents",
  "arguments": {
    "query": "如何申请访问权限",
    "category": "产品资料",
    "limit": 3
  }
}
```

```json
{
  "serviceVersion": "1.0.0",
  "dataVersion": "product-docs-v1",
  "items": [
    {
      "documentId": "doc_7h2...",
      "title": "接入说明",
      "summary": "访问前需要有效身份和被授予的服务权限。",
      "page": 7,
      "citation": "cit_9c2..."
    }
  ],
  "nextCursor": null
}
```

最终 MCP 不包含原文件 URL、存储凭证、其他项目 ID、未允许字段或内部模块配置。

## 15. 发布后运营

### 15.1 服务详情

标签页：概览、能力、数据、访问、凭证、运行、版本、审核、日志。

用户可以：

- 查看调用、成功率、延迟、拒绝和配额；
- 按版本、环境、Tool 和凭证筛选；
- 查看脱敏请求追踪；
- 轮换或撤销凭证；
- 创建新版本；
- 暂停/恢复调用；
- 停止发放新访问；
- 下线旧版本。

### 15.2 创建新版本

用户选择：复制生产版本、继续已有草稿或空白创建。页面始终提示“生产仍在运行 1.0.0，你正在编辑候选 1.1.0”。

提交前查看差异：

```text
数据：资料库 v1 → v2
模块：搜索模块 1.2.0 → 1.3.0
能力：无变化
输出：单次结果 10 → 20
访问：无变化
风险：输出范围扩大，需要重新审核
```

### 15.3 更新数据

```text
创建数据候选 → 上传/同步 → 处理 → 查看差异
→ 绑定到新服务版本 → 预览 → 测试 → 审核 → 发布
```

旧服务版本继续读取旧数据版本。

### 15.4 高影响操作

| 动作 | 现有客户端 | 新访问 | 运行服务 | 可恢复 |
| --- | --- | --- | --- | --- |
| 暂停调用 | 立即不能新调用 | 凭证状态不自动改变 | 暂停处理 | 可以 |
| 停止发放新访问 | 已有权限按规则继续 | 不再创建新凭证/授权 | 保持运行 | 可以 |
| 下线版本 | 按迁移计划处理 | 不再允许关联 | 最终停止 | 视保留策略 |

确认框显示受影响凭证、最近调用者数量、计划时间、恢复方式和通知范围。

## 16. 完整案例走查

案例：用户上传 `产品白皮书.pdf`、`API接入说明.docx`、`常见问题.xlsx`。

| 步骤 | 本例选择 | 形成结果 |
| --- | --- | --- |
| 创建 | “资料查询”模板 | 预选只读查询组合 |
| 目标 | 搜索、按段读取、验证引用 | 预计 5 Tools |
| 数据 | 上传 3 文件，固定版本 | 数据快照 v1 |
| 处理 | 3 文件、48 段、126 条记录 | 可搜索索引和稳定 sectionId |
| 模块 | 接受推荐组合 | Source/Capability/Output/Prompt 锁定版本 |
| 字段 | 查询 title/body，过滤 category/source | 输入 Schema |
| 输出 | 标题、摘要、页码、来源、引用 | 输出白名单 |
| 限制 | 10 条、5 段、总 4000 字 | 有效输出边界 |
| 访问 | OAuth/API Key，60 次/分钟，并发 5 | 访问和运行策略 |
| 预览 | 三视角确认 | 固定 preview digest |
| 测试 | 20/20，包括索要原文件负例 | 完整测试报告 |
| 发布 | 1.0.0，审核通过 | 生产 MCP URL |
| 凭证 | 创建受限 API Key | 客户端可连接并只见允许能力 |

完成时用户能够用一句话理解结果：

```text
获授权客户端可以在 3 份固定版本资料中搜索，读取最多 5 个相关段落，
并获得可验证引用；不能下载原文件、读取整篇、修改数据或访问其他项目。
```

## 17. 用户操作流程验收

- 首次用户不理解 Tool/Resource/Prompt 也能完成标准模板；
- 每个选择都有即时结果，不能到发布前才揭示影响；
- 每一步都能保存退出和恢复；
- 上传部分失败、连接失败、模块冲突、测试失败、审核退回均有恢复路径；
- 预览、测试、审核和运行的能力及限制一致；
- 未授权用户不能通过列表或错误探测数据；
- 发布后所有关键变化通过新版本完成；
- 凭证只显示一次并可轮换、撤销；
- 暂停、停止新授权和下线的含义清晰；
- 用户始终知道当前项目、环境、草稿/版本和保存状态。
