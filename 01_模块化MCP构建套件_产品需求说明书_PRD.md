# 模块化 MCP 构建套件产品需求说明书（PRD）

> 版本：V1.0  
> 日期：2026-09-15  
> 项目类型：全新独立产品  
> 状态：产品立项与设计基线  
> 适用对象：产品、设计、研发、测试、安全、运维和运营团队

---

## 1. 产品概述

### 1.1 产品名称

暂定名称：模块化 MCP 构建套件，英文暂定 `Modular MCP Studio`。

### 1.2 产品定位

这是一个面向数据拥有者、业务团队和开发者的 MCP 服务创建、测试、发布与运营平台。

用户不需要手写 MCP Server 代码，可以通过以下方式构建服务：

1. 上传或连接数据；
2. 选择服务目标；
3. 组合经过审核的 Source、Capability、Output 和 Prompt 模块；
4. 配置输入、数据范围、返回内容和访问规则；
5. 预览、自动测试并发布远程 MCP 服务；
6. 持续查看调用、错误、配额、版本和安全状态。

### 1.3 要解决的问题

- 业务用户不知道如何把数据转换为 MCP Tools、Resources 和 Prompts；
- 从数据连接、权限、Schema 到服务部署的技术链路过长；
- 手工 MCP 服务容易出现越权、返回过量、凭证泄露和版本漂移；
- 服务上线前缺少一致的预览、测试和安全检查；
- 数据、模块、授权和运行版本难以追踪；
- 发布后缺少调用洞察、异常处理、撤权和版本升级能力。

### 1.4 产品价值

| 用户 | 核心价值 |
| --- | --- |
| 数据拥有者 | 在不公开原始数据的情况下提供受控查询、读取或验证能力 |
| 业务产品经理 | 用业务语言设计 MCP 服务，不必先掌握协议代码 |
| 开发者 | 获得标准 Schema、调试器、版本、日志和可重复部署能力 |
| 安全/审核人员 | 在发布前查看数据、能力、权限、输出和测试证据 |
| 平台运营者 | 统一管理服务状态、风险、资源、用量和质量 |

## 2. 产品目标与指标

### 2.1 P0 产品目标

1. 新用户可以在 30 分钟内发布一个基于上传文档的只读 MCP 服务；
2. 用户在每个配置步骤都能理解“选择了什么”和“最终会产生什么”；
3. 未授权请求、越界读取、超配额和已撤权请求均被明确拒绝；
4. 预览、测试、审核和线上运行使用同一个服务定义；
5. 发布版本的数据、模块和能力可以完整追踪且不可静默变化；
6. 上传或构建失败后可以从失败项恢复，不要求全部重做；
7. 产品具备完整的服务运营和新版本发布流程。

### 2.2 核心指标

| 指标 | P0 目标 |
| --- | --- |
| 创建向导完成率 | ≥ 60% |
| 首次可预览中位耗时 | ≤ 20 分钟 |
| 首次完整测试通过率 | ≥ 70% |
| 发布一次成功率 | ≥ 95% |
| 上传任务可恢复率 | ≥ 99% |
| 未授权请求正确拦截率 | 100% |
| 跨项目或跨租户数据泄漏 | 0 |
| 发布后定义静默漂移 | 0 |
| 重大错误中可获得下一步操作的比例 | 100% |

## 3. 产品范围

### 3.1 P0 范围

- 用户注册、登录、团队和项目；
- 开发、测试、生产三个逻辑环境；
- 模板创建和空白创建；
- 上传文件、外部 HTTP API、对象存储、数据库只读连接；
- TXT、Markdown、PDF、DOCX、CSV、XLSX、JSON；
- 模块目录、推荐、依赖、冲突、风险和固定版本；
- Tools、Resources、Prompts 配置；
- 字段、条数、正文长度、速率、并发、超时和敏感字段保护；
- 三视角预览、快速测试、完整测试和自定义测试；
- 提交审核、修改、通过、拒绝、部署和发布；
- API Key 与 OAuth 两类客户端访问方式；
- 访问策略、角色、配额、有效期和撤权；
- 服务详情、请求追踪、统计、告警、版本和数据更新；
- 暂停调用、停止新授权、下线版本；
- 操作审计和基础安全事件。

### 3.2 P1 范围

- 自定义模块开发者中心和模块审核；
- Webhook、GraphQL、S3、主流数据库连接器扩展；
- 向量检索和语义搜索；
- 灰度发布和多区域部署；
- 套餐、计费和外部交易平台集成；
- 企业 SSO、SCIM 和高级审批流；
- 自定义域名和品牌化服务页。

### 3.3 非目标

- 运行用户提交的任意脚本或容器；
- 替代通用数据清洗、BI、ETL 或模型训练平台；
- 默认提供原文件下载或整库导出；
- 在 Prompt 中绕过 Tool 权限；
- 将生产凭证直接交给模块；
- 发布后直接覆盖已上线的数据或模块版本；
- P0 支持写数据库、执行交易或其他不可逆动作。

## 4. 用户角色与权限

| 角色 | 主要权限 |
| --- | --- |
| 个人用户 | 管理个人项目、数据、服务和凭证 |
| 团队所有者 | 管理团队、成员、计费配置和全部项目 |
| 项目管理员 | 管理项目成员、数据、策略、审核和部署 |
| 服务编辑者 | 编辑草稿、接入数据、配置模块和运行测试 |
| 发布者 | 提交审核、部署、暂停和创建生产版本 |
| 审核者 | 查看候选版本、测试证据并给出审核决定 |
| 只读观察者 | 查看配置、版本、运行和报告 |
| 平台运营者 | 管理平台模块、风险、资源和全局策略 |

权限采用团队角色和项目角色叠加，取更严格结果。发布、暂停、下线、扩大数据范围和扩大输出范围属于高影响操作。

## 5. 核心业务对象

| 对象 | 说明 |
| --- | --- |
| Workspace | 个人或团队的顶层隔离空间 |
| Project | 数据、草稿、服务、环境和成员的管理单元 |
| Data Source | 上传或连接的数据来源 |
| Data Version | 一次固定、可追踪的数据快照或连接版本 |
| Module | 可组合的 Source、Capability、Output 或 Prompt 功能单元 |
| Draft | 可编辑的服务配置 |
| Service Definition | 从草稿确定性生成的完整 MCP 服务描述 |
| Candidate | 已冻结并等待审核的定义 |
| Service | 面向用户长期存在的 MCP 服务 |
| Service Version | 一个不可变发布版本 |
| Deployment | 某个版本在某个环境中的运行实例 |
| Access Policy | 谁、何时、可使用什么、使用多少 |
| Credential | MCP 客户端连接服务的访问凭证 |
| Test Run | 针对固定 Definition 的测试执行与报告 |
| Request Trace | 一次 MCP 请求的脱敏阶段记录 |

## 6. 总体业务流程

```mermaid
flowchart LR
    signup["注册/登录"] --> workspace["创建或选择工作空间"]
    workspace --> project["创建项目"]
    project --> create["创建 MCP 草稿"]
    create --> goal["1 定义目标"]
    goal --> data["2 添加数据"]
    data --> modules["3 选择模块"]
    modules --> configure["4 配置能力与策略"]
    configure --> preview["5 三视角预览"]
    preview --> test["6 测试"]
    test --> submit["7 提交审核"]
    submit --> review{"审核结果"}
    review -->|"修改"| configure
    review -->|"通过"| deploy["部署"]
    deploy --> credential["创建访问方式"]
    credential --> operate["运营、告警和新版本"]
```

## 7. 功能需求

### 7.1 账户、团队与项目

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| ACC-001 | 支持邮箱/企业身份登录和退出 | P0 | 用户可建立、刷新和终止会话；失败有明确原因 |
| ACC-002 | 支持创建个人或团队 Workspace | P0 | 不同 Workspace 数据完全隔离 |
| ACC-003 | 支持邀请成员并分配角色 | P0 | 权限变更在下一次操作生效并记录审计 |
| ACC-004 | 支持创建、归档和恢复 Project | P0 | 归档后不接受新构建，但历史版本可查看 |
| ACC-005 | 支持开发/测试/生产环境 | P0 | 凭证、数据和部署按环境隔离 |
| ACC-006 | 高影响操作要求重新验证身份 | P0 | 发布、暂停、下线等操作触发二次验证 |

### 7.2 创建入口与模板

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| CRT-001 | 支持空白、模板和复制现有版本三种创建方式 | P0 | 三种方式都形成独立 Draft |
| CRT-002 | 模板显示适用场景、默认能力、数据要求和风险 | P0 | 选择前可查看完整影响摘要 |
| CRT-003 | P0 提供资料查询、数据验证、目录浏览三个模板 | P0 | 均可在无代码条件下完成构建 |
| CRT-004 | Draft 自动保存并支持退出恢复 | P0 | 刷新或重新登录后恢复上次步骤和服务器版本 |
| CRT-005 | 多人修改发生冲突时显示差异 | P0 | 不得静默覆盖另一用户修改 |

### 7.3 业务目标

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| GOL-001 | 用户可选择列表、元数据、搜索、按段读取和验证 | P0 | 选择结果即时改变推荐模块和最终能力摘要 |
| GOL-002 | 用户必须填写名称、说明、适用对象和禁止用途 | P0 | 缺少必填信息不能完成当前步骤 |
| GOL-003 | 写入、删除和原文件导出在 P0 不可选择 | P0 | 页面说明属于后续能力而非隐藏入口 |
| GOL-004 | 系统提示目标之间的依赖和冲突 | P0 | 冲突可定位并给出至少一种可执行方案 |

### 7.4 数据接入

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| DAT-001 | 支持文件上传并显示单文件进度 | P0 | 多文件并发，失败文件可单独重试 |
| DAT-002 | 支持断点续传、取消和过期恢复 | P0 | 成功分片不重复上传 |
| DAT-003 | 上传后完成扫描、格式识别、解析、规范化、切块和索引 | P0 | 每个阶段有状态和可定位错误 |
| DAT-004 | 支持 TXT、MD、PDF、DOCX、CSV、XLSX、JSON | P0 | 每种格式有固定样例和失败样例 |
| DAT-005 | 支持只读 HTTP API 连接 | P0 | 用户选择认证、路径、参数和响应字段；连接测试通过才可使用 |
| DAT-006 | 支持对象存储连接 | P0 | 只能访问用户配置并验证的 bucket/prefix |
| DAT-007 | 支持只读数据库连接 | P0 | 只能从允许的视图/表和字段中选择，不接受任意 SQL |
| DAT-008 | 数据凭证只在创建/更新时输入，后续不回显 | P0 | 页面和日志只显示凭证类型、末位和更新时间 |
| DAT-009 | 用户选择固定快照或受控更新策略 | P0 | 默认固定版本；更新不会改变已发布旧版本 |
| DAT-010 | 支持数据预览、字段选择、标题/分类修正 | P0 | 预览脱敏、分页并受角色控制 |
| DAT-011 | 处理部分失败时支持替换、排除或重试 | P0 | 未经确认不能把部分失败标记为完成 |
| DAT-012 | 删除数据时显示受影响服务和版本 | P0 | 已发布依赖存在时不能直接物理删除 |

### 7.5 模块目录与组合

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| MOD-001 | 模块分为 Source、Capability、Output、Prompt 四类 | P0 | 每个模块只承担声明的职责 |
| MOD-002 | 模块卡片显示名称、用途、版本、风险、依赖和配置要求 | P0 | 用户选择前可以理解影响 |
| MOD-003 | 系统根据目标和数据推荐模块组合 | P0 | 推荐结果可解释且可逐项修改 |
| MOD-004 | 自动识别缺失依赖、版本不兼容、冲突和循环依赖 | P0 | 阻断问题不能被忽略发布 |
| MOD-005 | 支持一键补齐依赖和查看替代方案 | P0 | 自动操作前展示新增模块和能力变化 |
| MOD-006 | 生产只允许使用审核通过的精确模块版本 | P0 | 模块更新不自动替换已发布版本 |
| MOD-007 | 平台安全模块始终启用且不可关闭 | P0 | 页面说明强制原因和作用 |
| MOD-008 | 被阻止模块触发已发布服务影响分析 | P0 | 可以停止新选择并定位受影响版本 |

### 7.6 Tools、Resources 和 Prompts

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| CAP-001 | 用户可查看和配置最终 Tools | P0 | 每个 Tool 显示技术名、中文说明、输入、输出和权限 |
| CAP-002 | 支持目录、元数据、搜索、按段读取、引用验证标准 Tool | P0 | 每个 Tool 有 Schema、示例和负例 |
| CAP-003 | 用户可选择查询字段和过滤字段 | P0 | 不可查询字段无法通过高级模式绕过 |
| CAP-004 | 用户可选择返回字段、条数和正文长度 | P0 | 结果预览即时反映变化 |
| CAP-005 | 按段读取默认最多 5 段，整篇和原文件默认关闭 | P0 | P0 无法通过客户端参数扩大范围 |
| CAP-006 | 支持集合、文档、章节 Resource Template | P0 | Resource 范围与 Tool 可触达范围一致 |
| CAP-007 | 支持检索回答、证据总结、引用验证 Prompt | P0 | Prompt 只编排当前可用 Tool，不新增权限 |
| CAP-008 | 能力和资源标识不可直接暴露存储路径或凭证 | P0 | 猜测地址不能访问其他数据 |

### 7.7 输出保护和运行策略

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| POL-001 | 用户配置字段白名单和敏感字段处理 | P0 | 未允许字段在任何结果和错误中都不出现 |
| POL-002 | 用户配置单次条数、单项长度和总响应大小 | P0 | 超限结果结构化截断或拒绝 |
| POL-003 | 用户配置速率、并发和超时 | P0 | 只能在平台上限内收紧 |
| POL-004 | 支持按用户、团队、客户端、时间和环境控制访问 | P0 | 策略变化在下一次请求生效 |
| POL-005 | 支持请求次数、读取字节等配额 | P0 | 超配额有稳定错误和重置提示 |
| POL-006 | 支持 API Key 和 OAuth 凭证 | P0 | 可创建、轮换、撤销；Secret 只展示一次 |
| POL-007 | 策略系统异常时默认拒绝受保护请求 | P0 | 不降级为公开访问 |
| POL-008 | 列表和发现能力也经过认证及可见性过滤 | P0 | 无权用户不能推测隐藏能力 |

### 7.8 预览和测试

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| PRE-001 | 提供所有者、使用者、MCP 客户端三种预览 | P0 | 三种视图来自同一候选定义 |
| PRE-002 | 所有者预览数据、模块、限制、风险和计量 | P0 | 每个值可追溯到选择来源 |
| PRE-003 | 使用者预览授权前后能力和错误 | P0 | 不展示内部存储和安全实现 |
| PRE-004 | 客户端预览真实 Tools/Resources/Prompts 与 JSON 示例 | P0 | Schema 与线上定义一致 |
| TST-001 | 支持快速、完整和自定义测试 | P0 | 测试绑定固定 Definition |
| TST-002 | 完整测试覆盖未授权、越界、超限、撤权和数据异常 | P0 | 缺少必测用例不能提交审核 |
| TST-003 | 支持后台测试、进度、失败定位和单项重跑 | P0 | 成功用例不会因单项重跑而丢失 |
| TST-004 | 测试报告可追踪版本、时间、结果和安全日志标识 | P0 | 报告不可覆盖，敏感正文不入报告 |

### 7.9 审核、部署和发布

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| PUB-001 | 提交前执行全量校验和确认清单 | P0 | 所有阻断项解决、完整测试通过才可提交 |
| PUB-002 | 提交后冻结 Candidate | P0 | 修改必须撤回或创建新修订 |
| PUB-003 | 审核覆盖业务、数据权利、技术和安全 | P0 | 意见定位到步骤和字段 |
| PUB-004 | 支持通过、需修改、补材料、拒绝和撤回 | P0 | 每种状态有明确下一步 |
| PUB-005 | 部署分阶段显示构建、加载、健康和开放 endpoint | P0 | 任一关键阶段失败不开放访问 |
| PUB-006 | 审核定义与运行定义摘要必须一致 | P0 | 不一致时阻止发布且无忽略入口 |
| PUB-007 | 发布后生成服务 URL、认证说明和能力摘要 | P0 | URL 旁明确说明仍需有效凭证和策略授权 |
| PUB-008 | 支持开发、测试、生产环境逐级发布 | P0 | 凭证和数据范围不跨环境复用 |

### 7.10 服务运营与版本

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| OPS-001 | 服务概览显示状态、版本、调用、错误、延迟和告警 | P0 | 指标可按环境和版本筛选 |
| OPS-002 | 提供能力、数据、策略、凭证、运行、版本、审核和日志页面 | P0 | 线上配置为只读可核验视图 |
| OPS-003 | 提供脱敏请求追踪 | P0 | 可定位阶段和错误，不显示 Secret/正文 |
| OPS-004 | 支持从任一历史版本创建新草稿 | P0 | 不复制访问凭证和实时配额状态 |
| OPS-005 | 提供模块、数据、能力、输出和策略差异 | P0 | 扩大范围被标记为高风险变化 |
| OPS-006 | 新数据形成候选数据版本并重新测试发布 | P0 | 不改变旧版本读取结果 |
| OPS-007 | 暂停调用、停止发放新访问和下线版本分开 | P0 | 三个动作影响和恢复方式不同 |
| OPS-008 | 高影响操作展示受影响客户端和恢复方式 | P0 | 二次确认并记录操作者、时间和原因 |
| OPS-009 | 支持凭证轮换和撤销 | P0 | 撤销后新请求立即拒绝 |
| OPS-010 | 支持站内通知和可配置外部通知 | P0 | 只通知有意义的状态变化 |

### 7.11 平台管理

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| ADM-001 | 管理标准模块及其审核状态 | P0 | 被阻止版本不能用于新发布 |
| ADM-002 | 管理模板、平台限制和风险规则 | P0 | 规则变化有版本和审计 |
| ADM-003 | 查看租户、任务、资源和运行健康 | P0 | 无权限运营者看不到数据正文 |
| ADM-004 | 管理异常服务和紧急暂停 | P0 | 操作可追溯并通知项目所有者 |
| ADM-005 | 查看安全事件、审计和保留策略 | P0 | 记录不可被普通用户修改 |

## 8. 业务规则

### 8.1 数据规则

1. 数据只能由用户上传或由用户创建并验证的数据连接获取；
2. 每个服务版本绑定明确数据版本；
3. 连接更新不能静默扩大字段、记录或访问范围；
4. 原始数据默认不向 MCP 客户端返回；
5. 数据预览、测试、日志和示例遵循同一敏感字段策略；
6. 删除已发布依赖数据前必须先处理关联版本和访问权益。

### 8.2 模块规则

1. 生产模块必须审核、签名并锁定精确版本；
2. 用户不能通过配置开启模块未声明的权限；
3. 模块依赖、冲突和风险必须在提交前解决；
4. 强制安全层不作为用户可关闭模块展示；
5. 模块被阻止时先停止新选择，再分析已发布服务影响。

### 8.3 访问规则

1. 每个 MCP 请求都需要有效身份；
2. 初始化、列表、读取和调用都需要访问策略判断；
3. 凭证只是身份入口，不代表自动拥有全部服务能力；
4. 策略按服务版本、能力、数据范围、环境、有效期和配额判断；
5. 授权、配额或依赖服务无法确认时默认拒绝；
6. 撤销凭证或策略后，下一次新请求必须被拒绝。

### 8.4 版本规则

1. Draft 可编辑；Candidate 冻结；Published Version 不可变；
2. 数据、模块、Tool Schema、输出范围和策略范围变化均创建新版本；
3. 文案和无行为变化的展示信息可按变更策略单独管理；
4. 所有版本可比较、追踪、回滚流量，但历史事实不覆盖；
5. 运行加载内容必须与已批准 Definition 一致。

## 9. 关键状态机

```text
Draft: editing -> validating -> ready -> building -> built -> submitted
Candidate: submitted -> changes_requested | materials_required | rejected | approved | withdrawn
Version: approved -> deploying -> published -> suspended -> retired
Deployment: provisioning -> healthy <-> degraded -> draining -> suspended -> stopped | blocked
Upload: created -> uploading -> uploaded -> processing -> completed | partial | failed | cancelled | expired
Credential: active -> rotating -> revoked | expired
Module Version: draft -> testing -> submitted -> approved -> deprecated | blocked
```

所有状态转换必须校验前态、角色和影响范围，并写入审计。

## 10. 标准错误与异常

| 场景 | 用户结果 | 恢复操作 |
| --- | --- | --- |
| 会话过期 | 页面只读，未保存内容本地保留 | 重新登录并返回原步骤 |
| 上传中断 | 显示已完成分片 | 继续、取消或稍后恢复 |
| 文件被隔离 | 不进入解析和索引 | 替换、移除或申诉 |
| 部分解析失败 | 显示文件/页/表/字段 | 重试、替换或确认排除 |
| 数据连接失败 | 显示认证、网络、Schema 分类 | 更新凭证或配置后测试 |
| 模块缺依赖 | 阻止继续 | 一键补齐或取消能力 |
| 模块冲突 | 比较两个解决方案 | 关闭其一或调整目标 |
| 配置超过平台上限 | 字段级阻断 | 调整为允许值 |
| 完整测试失败 | 提交按钮不可用 | 修复并重跑受影响测试 |
| 审核退回 | 定位到步骤和字段 | 创建修订并重新提交 |
| 部署定义不一致 | 阻止开放 endpoint | 重新构建并调查制品 |
| 无访问授权 | MCP 请求拒绝 | 获取权限或更换凭证 |
| 超出配额 | 请求拒绝并显示重置提示 | 等待重置或调整配额 |
| 服务暂停 | 请求拒绝 | 管理员恢复服务 |

错误消息统一包含：发生了什么、影响什么、下一步动作和请求编号。无权错误不泄露资源是否存在。

## 11. 非功能需求

### 11.1 安全

- NFR-SEC-001：所有业务数据按 Workspace、Project、Environment 隔离；
- NFR-SEC-002：凭证加密保存，只在创建时显示明文一次；
- NFR-SEC-003：上传先隔离扫描，解析器限制 CPU、内存、时间和文件展开大小；
- NFR-SEC-004：外部连接防止 SSRF、路径穿越、重定向绕过和私网探测；
- NFR-SEC-005：模块不可直接获取生产 Secret、文件系统或任意网络权限；
- NFR-SEC-006：日志、追踪和分析不保存原始正文和完整凭证；
- NFR-SEC-007：高影响操作二次验证并保留不可篡改审计；
- NFR-SEC-008：授权异常时关闭式拒绝；
- NFR-SEC-009：跨租户、越权、重复请求和撤权场景有自动安全测试。

### 11.2 性能与容量

| 项目 | P0 目标 |
| --- | --- |
| 工作台和向导普通 API P95 | ≤ 800 ms |
| 已索引搜索 P95 | ≤ 2 s |
| 按段读取 P95 | ≤ 3 s |
| 单文件默认上限 | 100 MB，可由平台配置 |
| 单草稿文件数量 | 20，可由平台配置 |
| 单次搜索结果 | 默认 10，平台最大值受控 |
| 单次读取段落 | 默认和最大均为 5 |
| 普通 MCP 请求默认超时 | 15 s |
| 上传与构建任务 | 后台执行，不依赖页面打开 |

### 11.3 可用性与可靠性

- 服务月度目标可用性 99.9%；
- 后台任务至少一次执行但业务结果幂等；
- 重试不能重复创建版本、重复部署或重复计量；
- 关键状态写入成功后通知失败不丢失，支持补发；
- 发布和撤权路径优先于非关键统计；
- 备份恢复后 Definition、策略、凭证状态和审计可核对。

### 11.4 可观测性

- 每个请求和任务具有 requestId/jobId/traceId；
- 可查看认证、策略、数据、能力、输出和响应各阶段耗时；
- 提供错误率、延迟、拒绝原因、配额、任务积压和部署健康指标；
- 告警包含影响服务、环境、开始时间和建议动作；
- 不在指标标签中写高基数正文、查询内容或凭证。

### 11.5 可访问性与兼容性

- 核心流程满足 WCAG 2.2 AA；
- 支持键盘完成创建、配置、测试和发布；
- 状态不只依赖颜色；
- 支持当前主流桌面浏览器最近两个稳定版本；
- 小屏支持查看、审批和紧急暂停，复杂配置明确提示桌面使用。

## 12. 分析与埋点

核心事件：注册完成、创建开始、模板选择、目标保存、数据接入开始/完成、处理失败、模块选择、配置校验、预览确认、测试完成、审核提交、审核完成、部署完成、凭证创建、首次成功调用、新版本创建和暂停。

埋点禁止包含文件正文、查询全文、Secret、Token、完整 URL 参数和个人敏感信息。

## 13. 端到端验收任务

1. 上传 3 份资料并发布受控查询 MCP；
2. 3 个文件中 1 个失败，用户只修复失败项并继续；
3. 选择引用模块，补齐依赖并解决一个冲突；
4. 审核退回输出过长配置，用户定位修改并重提；
5. 发布后上传数据 v2，比较差异并发布新版本；
6. 无凭证、无能力权限、超配额和凭证撤销调用均正确拒绝；
7. 两个 Workspace 使用相同资源名称也不能互相发现；
8. 修改线上引用的数据或模块不会改变旧版本；
9. 暂停调用、停止新授权和下线版本产生不同结果；
10. 所有错误均提供可执行恢复动作且不泄露敏感信息。

## 14. 发布计划

| 里程碑 | 交付 |
| --- | --- |
| M0 产品与原型 | PRD、用户流程、UI 系统、可点击主链路 |
| M1 纵向切片 | 上传 TXT/PDF、元数据/验证 Tool、测试、发布、API Key |
| M2 资料查询 | 七格式、搜索、按段读取、Resources、Prompts |
| M3 多数据源 | HTTP、对象存储、数据库只读连接 |
| M4 运营闭环 | OAuth、策略、配额、告警、版本、暂停和审计 |
| M5 生产候选 | 安全、性能、灾备、完整 E2E 和试点 |

## 15. 开发前待确认事项

这些选择不依赖任何现有系统，应在新项目技术设计阶段做出并记录：

1. 首期部署形态是 SaaS、私有化，还是两者兼容；
2. 默认身份提供方和企业 SSO 范围；
3. OAuth 的默认身份提供方、客户端注册方式和企业身份接入边界；
4. 正式对象存储、检索引擎和文档解析器；
5. 数据库存储连接首批支持的数据库类型；
6. 模块制品格式和签名信任根；
7. 审核是项目内部审核还是平台强制审核；
8. 数据、日志、审计和备份的默认保留期限；
9. 各套餐或部署形态的资源和配额上限；
10. 品牌名称、视觉规范和面向外部用户的术语。

未确认的高风险能力维持关闭，不阻止“上传资料构建只读查询 MCP”的首个纵向切片。

## 16. PRD 完成定义

- 所有 P0 需求均有唯一编号和验收标准；
- 三份移交文档中的步骤、角色、状态和术语一致；
- 原型覆盖第 13 节至少前 5 条任务；
- 技术方案逐项映射全部 P0 编号；
- 测试计划覆盖正常、异常、权限、撤权、并发和恢复；
- 任何范围变化先修改 PRD 并记录版本，不以口头约定替代。
