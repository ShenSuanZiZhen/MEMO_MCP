{
  "name": "modular-mcp-suite",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.4.1",
  "engines": {
    "node": ">=22 <23",
    "pnpm": ">=12 <13"
  },
  "scripts": {
    "bootstrap": "pnpm install --frozen-lockfile",
    "lint": "node scripts/lint-workspace.mjs",
    "format": "prettier --check README.md AGENTS.md package.json pnpm-workspace.yaml tsconfig.base.json tsconfig.packages.json vitest.config.ts \"apps/**/*.{json,ts}\" \"packages/**/*.{json,ts}\" \"scripts/**/*.mjs\" \"tests/**/*.ts\" \"docs/work-package-evidence/**/*.md\"",
    "typecheck": "tsc --build tsconfig.packages.json",
    "test": "vitest run",
    "build": "pnpm -r --if-present build",
    "verify:affected": "pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "verify": "pnpm verify:affected"
  },
  "devDependencies": {
    "@types/node": "22.18.6",
    "prettier": "3.6.2",
    "typescript": "5.9.2",
    "vitest": "3.2.4"
  }
}
